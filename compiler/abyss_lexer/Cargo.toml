[package]
name = "abyss_lexer"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
abyss_diagnostics = { path = "../abyss_diagnostics" }
abyss_token = { path = "../abyss_token" }
