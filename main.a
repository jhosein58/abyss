
-- complex_math = -a.b + *ptr[0] * ~2 ^ 3 == 4 and true or false

-- result = matrix[get_row(x)](arg).process

-- crazy_val = if a > b {
--     a + 1
-- } else {
--     (if c == d { 10 } else { 20 }) * 5
-- }

-- Handler :: [
--     id: i32,
--     make_cb: (timeout: i32): (msg: &str): bool _ {
--         ret (msg: &str): bool {
--             msg == "die"
--         }
--     }
-- ]

-- calc = forever {
--     if check() { 
--         out 26
--     }
--     next
-- }


add :: (a: i32, b: i32): i32 {
    ret a + b
}

print(add(2, 3, 3, 5))
