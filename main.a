-- def fib(n: i32): i32 {
--     if n < 2 {
--         n
--     } else {
--         fib(n - 1) + fib(n - 2)
--     }
-- }

-- def generate_array(arr: [i32; 10000], s: i32) {
--     seed := 12345
--     i := 0
    
--     while i < s {
--         seed = (seed * 1103515245 + 12345)
        
--         val := seed
--         if val < 0 {
--             val = 0 - val
--         }
        
--         mod_val := val - (val / 10000) * 10000
        
--         arr[i] = mod_val
--         i = i + 1
--     }
-- }

-- def partition(arr: [i32; 10000], low: i32, high: i32): i32 {
--     pivot := arr[high]
--     i := low - 1
--     j := low

--     while j < high {
--         if arr[j] < pivot {
--             i = i + 1
            
--             temp := arr[i]
--             arr[i] = arr[j]
--             arr[j] = temp
--         }
--         j = j + 1
--     }
    
--     temp2 := arr[i + 1]
--     arr[i + 1] = arr[high]
--     arr[high] = temp2

--     i + 1
-- }

-- def quicksort(arr: [i32; 10000], low: i32, high: i32) {
--     if low < high {
--         pi := partition(arr, low, high)
        
--         -- quicksort(arr, low, pi - 1)
--         -- quicksort(arr, pi + 1, high)
--     }
-- }

-- fib_result := fib(35)
-- print(fib_result)

-- s := 10000
-- arr := [0; 10000]
-- iter := 0

-- while iter < 100 {
--     generate_array(arr, s)
--     quicksort(arr, 0, s - 1)
--     iter = iter + 1
-- }

-- print(arr[5000])



-- def b 2
-- a := [10; { 
--     i := 0
--     while i < 100 i = i + 1 else i
-- }]


-- def ty(a: bool): type {
--     if a i32 else [f32; 100]
-- }

-- def arr [true, false]

-- def test (): ty(arr[0]) { 

-- }

-- arr[0] = false


-- print(arr[0])

-- def Pos [
--     x: f32,
--     y: f32
-- ]




-- a := [x: 10., y: 12.]

-- def inc(pos: [x: f32, y: f32]) {
--     pos.x = pos.x + 1.
-- }

-- inc(a)
-- print(a.x)


-- #inline(Always)
-- def mul (a: i32): i32 { a * 2 }

-- test :=  1 + mul(2)



-- a := 3

-- {
--     a := 2
-- }

-- print(a)

-----------------------------------------

-- def ty(): type { i32 }

-- a := 1. as ty()
----------------------------------------

-- def Pos [
--     x: f32,
--     y: f32
-- ]

-- def test1(pos: [x: f32, y: f32]) {
--     print(pos.x)
-- }

-- def test2(pos: Pos) {
--     print(pos.x)
-- }

-- pos := [x: 10., y: 12.] as Pos
-- fake_pos := [x: 10., y: 12.] 

-- test1(pos)       -- works
-- test1(fake_pos)  -- works

-- test2(pos)       -- works
-- test2(fake_pos)  -- Error!

-- def Meter i32

-- def number(any: i32) {
--     print(any)
-- }

-- def meter(meter: Meter) {
--     print(meter)
-- }

-- m := 100 as Meter
-- n := 100

-- number(m)  -- works
-- number(n)  -- works

-- meter(m)  -- works
-- meter(n)  -- Error!

----------------------------------------------------------------

-- def Pos [
--     x: f32,
--     y: f32,
-- ]


-- def Pos.test (self: [x: f32, y: f32 ]): Pos  {
--     self
-- }


-- def [x: f32, y: f32 ].test2() {
    
-- }

-- a := [x: 0., y: 1.] as Pos

-- a.test()

-- a.test2()

-- [x: f32, y: f32 ].test2()

-------------------------------------------------------------------

-- def printiln(i: i32) _

-- def Pos [
--     x: f32,
--     y: f32,
-- ]


-- def Pos.test (self: &Pos): Pos  {
--     self.x = (self.x + 1.)
--     self
-- }


-- def Cat [
--     id: i32,
--     age: i32
-- ]

-- def Cat.new(): Cat {
--     [id: 0, age: 2] as Cat
-- }

-- def Dog [
--     id: i32,
--     age: i32
-- ]

-- def Dog.new(): Dog {
--     [id: 1, age: 5] as Dog
-- }


-- def print_age(self: [id: i32, age: i32]) {
--     printiln(self.age)
-- }


-- cat := Cat.new()
-- dog := Dog.new()

-- cat.print_age()
-- dog.print_age()

-----------------------------------------------------------
-- def printi (s: i32) _
-- a := 10

-- b := &a

-- *b = 2

-- printi(a)
-- printi(*b)
-----------------------------------------------------------
--  def printi (s: i32) _

-- def Val [v: i32]

-- def test(val: &Val) {
--     val.v = val.v + 1
-- }

-- new_v := [v: 100] as Val

-- new_v.test()

-- printi(new_v.v)

------------------------------------------------------------
-- def print (s: &i32) _



-- i := 0
-- while i < 10 {
--     j := 0
--     while j < i {
--         print("*")
--         j = j + 1
--     }
--     print("\n")
--     i = i + 1
-- }


------------------------------------------------------------
-- def print (s: &i32) _
-- def printi (s: i32) _
-- def printf (s: f32) _

-- def i32.print(self: i32) {
--     printi(self)
-- }

-- def f32.print(self: f32) {
--     printf(self)
-- }


-- a := 3
-- 10.print()
----------------------------------------------------------

-- def print (str: &i32) _
-- def printi (i: i32) _
-- def printf (f: f32) _
-- def alloc(s: i32): &i32 _
-- def free(ptr: &i32) _

-- ptr := alloc(100)

-- ptr[0] = 3

-- free(ptr)

-------------------------------------------------------

-- def printiln (i: i32) _

-- MyI32.test()

-- def MyI32.test () {
--     printiln(100)
-- }


-- def MyI32 i32

-- printiln(const_arr[0])

-- def const_arr [1, 2, 3] 
------------------------------------------------------

-- def printiln(i: i32) _

-- bootstrap_system()

-- def bootstrap_system() {
--     manager := Manager.new()
--     manager.execute_task(CONFIG_CODES[2]) 
-- }

-- def execute_task(self: [worker: Worker, id: i32], code: i32) {
--     self.worker.do_work(code + MAGIC_NUMBER)
-- }

-- def Manager [
--     worker: Worker,
--     id: i32
-- ]

-- def Manager.new(): Manager {
--     [worker: Worker.new(), id: MAGIC_NUMBER] as Manager
-- }

-- def Worker [
--     status: i32
-- ]

-- def Worker.new(): Worker {
--     [status: 10] as Worker
-- }

-- def Worker.do_work(self: [status: i32], code: i32) {
--     printiln(code + self.status)
-- }

-- def CONFIG_CODES [100, 200, 300]
-- def MAGIC_NUMBER 42
------------------------------------------------------------

-- def printiln(i: i32) _

-- start_test()

-- def start_test() {
--     step_one(10)
--     printiln(VAL_B) 
-- }


-- def VAL_B VAL_A.a + 15

-- def step_one(x: i32) {
--     step_two(x + VAL_A.b) 
-- }

-- def step_two(y: i32) {
--     step_three(y)
-- }

-- def step_three(z: i32) {
--     printiln(z) 
-- }

-- def VAL_A [a: 10, b: 30]
-------------------------------------------------------------
-- def VAL_X VAL_Y + 1
-- def VAL_Y VAL_X + 1

-- def start() {
--     printiln(VAL_X)
-- }
------------------------------------------------------------

-- def println (s: &i32) _

-- def i32.b() {
--     println("outer b")
-- }

-- def a () {
--     def i32.b () {
--         println("inner b")
--     }
--     i32.b()
 
--     println("a")
-- }
-- i32.b() 
-- a()

----------------------------------------------------------

-- -- 04_def_resolution_torture.abyss

-- def printiln (i: i32) _
-- def printbln (b: bool) _

-- -- =====================================================================
-- -- 1. THE ULTIMATE OUT-OF-ORDER EXECUTION
-- -- Call the main logic before ANY of the types, constants, or methods 
-- -- are textually defined in the file!
-- -- =====================================================================
-- run_abyss_simulation()


-- -- =====================================================================
-- -- 2. THE MAIN TEST FUNCTION (Nested Hell)
-- -- =====================================================================
-- def run_abyss_simulation() {
--     printiln(999_999) -- Expected: 999999 (Start Marker)

--     -- A. Test Arrays and Out-of-Order Constant Resolution
--     printiln(MAGIC_CONFIG[1]) -- Expected: 42 (Resolves CALC_MAGIC out of order)

--     -- B. Test Hard Alias and Global Method Resolution
--     code := 100 as StatusCode
--     code.print_status()       -- Expected: 404 (Calls Global StatusCode.print_status)

--     -- C. Test Nested Scope Shadowing for Methods (The exact bug you just fixed!)
--     def StatusCode.print_status() {
--         printiln(777)         -- Local shadow of the method!
--     } 
--     code.print_status()       -- Expected: 777 (Must call the local version)

  

--     def calculate_dmg(power: i32, armor: i32): i32 {
--         -- Uses local constant `hidden_multiplier` and global `GLOBAL_BUFF`
--         ((power * 3) + GLOBAL_BUFF) - armor
--     }

--     -- E. Struct Instantiation with Hard Aliases & Out-of-Order Configs
--     player := Player.new()
--     boss := Boss.new()

--     -- F. Structural Methods (Duck Typing) & Pointer Mutation
--     -- `player` and `boss` both use the generic `buff_hp` and `take_damage` methods!
--     player.buff_hp(50)
--     boss.buff_hp(100)
    
--     -- -- G. Execute Combat!
--     -- dmg := calculate_dmg(player.power, boss.armor) -- (15 * 3) + 10 - 5 = 50
--     -- boss.take_damage(dmg)                          -- 600 - 50 = 550

--     -- -- Verify final states
--     -- printiln(player.hp as i32) -- Expected: 1050 (1000 + 50)
--     -- printiln(boss.hp)          -- Expected: 550  (500 + 100 - 50)
-- }


-- -- =====================================================================
-- -- 3. HARD TYPE ALIASES
-- -- =====================================================================
-- def StatusCode i32
-- def HealthPoint i32

-- -- Global Method for Hard Alias
-- def StatusCode.print_status() {
--     printiln(404)
-- }


-- -- =====================================================================
-- -- 4. STRUCTS & DUCK TYPING (Trait-like behavior)
-- -- =====================================================================
-- def Player [
--     id: StatusCode,     -- Field using Hard Alias
--     hp: HealthPoint,    -- Field using Hard Alias
--     power: i32
-- ]

-- def Boss [
--     hp: i32,            -- Normal i32 (testing duck typing compatibility)
--     armor: i32,
--     is_active: bool
-- ]


-- -- =====================================================================
-- -- 5. METHODS & MUTATION 
-- -- =====================================================================
-- def Player.new(): Player {
--     [
--         id: INITIAL_ID as StatusCode, 
--         hp: STARTING_HP as HealthPoint, 
--         power: BASE_POWER
--     ] as Player
-- } 

-- def Boss.new(): Boss {
--     [
--         hp: ENEMY_STATS[0], 
--         armor: ENEMY_STATS[1], 
--         is_active: true
--     ] as Boss
-- }

-- -- "Duck typed" method: Accepts ANY struct with an `hp` field via pointer
-- def buff_hp(self: &[hp: i32], amount: i32) {
--     self.hp = self.hp + amount
-- }

-- -- "Duck typed" method: Accepts ANY struct with an `hp` field via pointer
-- def take_damage(self: &[hp: i32], dmg: i32) {
--     self.hp = self.hp - dmg
-- }


-- -- =====================================================================
-- -- 6. TANGLED GLOBAL CONSTANTS AND ARRAYS (Circular Dependency Checks)
-- -- =====================================================================
-- def STARTING_HP 1000
-- def BASE_POWER MAGIC_CONFIG[0] + 5  -- 10 + 5 = 15
-- def INITIAL_ID 200

-- def GLOBAL_BUFF 10

-- def ENEMY_STATS [
--     500, -- Base HP
--     5    -- Base Armor
-- ]

-- -- -- Array depending on a function evaluated at compile-time/runtime
-- def MAGIC_CONFIG [
--     10,
--     CALC_MAGIC()
-- ]

-- def CALC_MAGIC(): i32 {
--     42
-- }
----------------------------------------------------

-- def print (i: i32)      _
-- def abs   (n: i32): i32 _

-- -- i := 0

-- -- while i < 100_000_000_000 {
   
-- --     i = i + 1
-- -- }
--  print(abs(-3))
-- -- print(PI)

--------------------------------------------------

-- def print (i: i32)  _

-- print(100)

-- a := c"A"

-- def print_f32(n: f64) _

-- print_f32(2. as f64 + 1. as f64)
-- print_f32(2. as f64 * 1. as f64)
-- print_f32(2. as f64 + 1. as f64)
-- print_f32(0.2 as f64)

--------------------------------------------------
-- def print_i32(i: i32) _

-- a := [3, 4, 5, 6]

-- print_i32(a[3])
--------------------------------------------------


2 + 2
